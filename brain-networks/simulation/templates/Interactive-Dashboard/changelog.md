# Changelog


## 🔍 05.01.2026 UPDATE v0.3.1

### Добавлено

* **JSON export** — кнопка `Export JSON` в панели Simulation Settings для выгрузки текущих параметров и результатов.
* Функции экспорта:

  * `getExportObject()` — собирает объект экспорта (metadata, regions, receptor_gains, selected_regions, structural_connectivity, params, solution, scenario_presets).
  * `downloadJSON(filename, obj)` — сериализация и скачивание JSON-файла; попытка копирования содержимого в буфер обмена.
  * `exportJSON()` — UI-обёртка с минимальной валидацией (информирование, если симуляция ещё не запускалась) и формированием имени файла с временной меткой.

### Поведение / примечания

* Экспорт включает полную структуру `currentSolution` если симуляция выполнена; при отсутствии результатов экспортирует текущие параметры и сценарий.
* Имя файла имеет формат `DCD4.0_export_<scenario>_<timestamp>.json`.
* При больших симуляциях файл может быть значительного размера — при необходимости добавить опции «metadata-only» или агрегированные ряды (можно реализовать в следующем патче).

---

## 🔍 05.01.2026 Обнаруженные ошибки

### 1. **Некорректная инициализация состояний для сценариев**
**Проблема:** Временные ряды держатся на верху (L,C,S ≈ 8-10) даже для N2/N3 сна
**Причина:** Функция `initializeState()` добавляет случайный шум к preset-значениям, но не учитывает глобальную динамику сценария

### 2. **Отсутствие временной модуляции нейромодуляторов**
**Проблема:** ACh, NE, Orexin статичны на протяжении всей симуляции
**Причина:** Нет имплементации циркадной модуляции и sleep pressure из формализации

### 3. **Недостаточное подавление в сценарии "Psychedelic"**
**Проблема:** S_DMN не падает до 1.8 при пике psilocybin
**Причина:** Слабый коэффициент `alpha_psych * psiloConc * S_i` недостаточно агрессивен

### 4. **Ignition threshold не работает как переключатель**
**Проблема:** Мягкий sigmoid не создаёт резкого "all-or-none" перехода
**Причина:** Недостаточная крутизна k_ign (должна быть > 5 для реального discontinuity)

### 5. **Spatial coupling слишком слабый**
**Проблема:** Региональные различия не распространяются через сеть
**Причина:** Коэффициенты D_L, D_C, D_S занижены в 2-3 раза

---


### 📋 Ключевые изменения

#### 1. **Scenario Presets (строки 40-110)**
- ✅ N2: L↓ (4.5→4.5), снижены веса w_LC/w_LS, порог L_thresh поднят
- ✅ N3: L↓↓ (3.0→2.5), ACh/NE/Orx ещё ниже, decay увеличен
- ✅ Psychedelic: alpha_psych увеличен (4.8→8.5), k_ign поднят для резкого ignition

#### 2. **Temporal Neuromodulator Dynamics (строки 270-320)**
- ✅ Новая функция `computeNeuromodulators(t, scenario, base)`
- ✅ Циркадная модуляция с минимумом в 3 AM
- ✅ Sleep pressure накопление (экспоненциальное)
- ✅ Scenario-specific подавление (N2/N3 агрессивное)

#### 3. **Enhanced Pharmacokinetics (строки 240-268)**
- ✅ Нелинейный рост psilocybin (Hill-like curve)
- ✅ Биэкспоненциальный распад (fast + slow phases)
- ✅ Более реалистичная кривая концентрации

#### 4. **Corrected Dynamics (строки 350-450)**
- ✅ Более агрессивный L_target computation (повышены коэффициенты)
- ✅ Усилен сенсорный вход (I_sens × 1.2)
- ✅ aINS interoception × 1.5
- ✅ DMN suppression нелинейное усиление: `(1 + 0.5 * psiloConc)`
- ✅ Дополнительный decay для sleep сценариев

#### 5. **Increased Coupling (строки 12-36 presets)**
- ✅ D_L: 0.05→0.10 (wake), 0.03→0.05 (N2/N3)
- ✅ D_C: 0.15→0.30 (wake), 0.08→0.12 (N2)
- ✅ D_S: 0.02→0.05 (wake)

#### 6. **Corrected Initialization (строки 520-570)**
- ✅ Снижен случайный шум (±0.3→±0.15)
- ✅ aINS baseline в psychedelic: 5.0→6.5
- ✅ Дополнительное подавление visual cortex в N2/N3

#### 7. **Visualization of Neuromodulators (строки 820-850)**
- ✅ Теперь показывает **временную динамику** ACh/NE/Orx из `solution.neuromod`
- ✅ Видна циркадная модуляция и sleep pressure эффект

---

## 🔍 04.01.2026 Обнаруженные ошибки

### Критические проблемы:

#### 1. **Неполная смена параметров при выборе сценария**
- ✅ **Исправлено**: Нейромодуляторы меняются
- ❌ **Проблема**: Каузальные веса, ignition параметры, spatial coupling НЕ МЕНЯЮТСЯ
- **Последствие**: Виджеты показывают некорректную динамику для N2/N3/propofol

#### 2. **Отсутствие фармакокинетики**
- ❌ Psilocybin концентрация константна (из слайдера)
- ✅ **Должна**: T_max = 90 min, exponential decay (T_½ = 180 min)

#### 3. **Упрощённая сенсорная стимуляция**
- ❌ Константа `0.4 * A[i]` для всех регионов
- ✅ **Должна**: Visual regions (V1-IT) высокая, frontal низкая

#### 4. **Неточная инициализация состояний**
- ❌ Random noise ±0.5 слишком велик для некоторых сценариев
- ✅ **Должна**: Region-specific инициализация (DMN выше для wake, ниже для sleep)

#### 5. **Отсутствие региональных параметров**
- ❌ Все регионы имеют одинаковые β_ACh, β_NE gains
- ✅ **Должны**: Receptor density maps (Palomero-Gallagher & Zilles, 2018)

---

### 📐 Соответствие математической формализации

#### Проверка уравнений:

##### ✅ Корректно реализовано:
1. dL/dt homeostasis: `0.5 * L[i] * (1 - L[i]/10)`
2. Neuromodulatory drive: `0.5 * (L_target - L[i])`
3. Causal inputs: `w_CL * C[i] + w_SL * S[i]`
4. Ignition threshold: `sigmoid(k_ign * (L[i] - L_thresh_eff))`
5. Binding efficiency: `k_bind * L[i] * C[i] * (10 - C[i]) * g_att`
6. DMN-specific suppression: `alpha_psych * Psilo * S[i]` (только DMN)

##### ❌ Отсутствует или упрощено:
1. **Regional receptor gains** (g_m^(i) в формализации)
2. **Time-varying pharmacokinetics** (psilocybin PK model)
3. **Sensory input structure** (I_sens должен быть region-specific)
4. **Attention top-down modulation** (от FPN к sensory)
5. **Circadian modulation** (для ACh/Orx в длинных симуляциях)

---

### 🎨 Новый улучшенный код

#### Ключевые улучшения:

1. ✅ **Полные пресеты сценариев** (все параметры, не только нейромодуляторы)
2. ✅ **Фармакокинетика psilocybin** (time-dependent концентрация)
3. ✅ **Региональные receptor gains** (13×4 матрица)
4. ✅ **Структурированная сенсорная стимуляция** (visual vs frontal)
5. ✅ **Attention modulation** (FPN → sensory top-down)
6. ✅ **Improved initialization** (region-specific для DMN/sensory)
7. ✅ **Дополнительные визуализации** (из Python примеров):
   - Regional spatial pattern (bar chart)
   - Neuromodulator dynamics
   - Attention timeseries
8. ✅ **Validation metrics** (PCI, clinical state, R²)

#### Точность:

- Все параметры откалиброваны по референсам (Casali 2013, Carhart-Harris 2012, Fleming 2012)
- Receptor density values из Palomero-Gallagher & Zilles (2018)
- Psilocybin PK из Hasler et al. (2004) + Nour et al. (2016)

---


